.. currentmodule:: mutiny

.. _api-client:

Client
======

The client manages gateway connection, state and is used as the main entry point
of the application.

.. autoclass:: Client
    :members:
    :exclude-members: listen

    .. automethod:: listen
        :decorator:
